# Input BERSAMA untuk semua .conf pipeline di folder ini -- pola sama dengan
# Sesi 8 (Data Ingestion): Filebeat kirim lewat beats input, Logstash tidak
# pernah sentuh filesystem host secara langsung.
input {
  beats {
    port => 5044
  }
}
