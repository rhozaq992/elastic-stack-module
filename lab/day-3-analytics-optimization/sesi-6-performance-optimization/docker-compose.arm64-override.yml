# WAJIB kalau laptopmu ARM (Apple Silicon) -- sama seperti override di
# Sesi 4 (lihat file itu untuk penjelasan lengkap kenapa `mysql` perlu
# ini). Sesi 6 bawa stack Robot Shop-nya sendiri, jadi override-nya perlu
# didaftarkan ulang di sini juga.
services:
  mysql:
    platform: linux/amd64
